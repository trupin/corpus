{
  "name": "corpus-plugin-fixture",
  "private": true,
  "version": "0.5.0",
  "type": "module",
  "description": "Test-only fixture plugin exercising every PLUGINS-001 extension point. The leading underscore excludes it from production surfaces (SPEC.md §10).",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@corpus/contract": "*",
    "@corpus/kit": "*",
    "hono": "^4.12.32",
    "zod": "^4.4.3"
  },
  "peerDependencies": {
    "react": "^19.2.8"
  },
  "devDependencies": {
    "@types/react": "^19.2.18",
    "react": "^19.2.8"
  }
}
