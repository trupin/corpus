# Evaluation: CLI-008

**Date**: 2026-07-27
**Sprint**: sprint-008
**Verdict**: PASS

Criteria: **15 / 15 PASS** (TEST-126 … TEST-140).

Evaluated against branch `phase-3-ui`, CLI-008 landed as `28fe8c1 [CLI-008] CLI hardening batch:
five PR #9 items`. All verification used the from-source entry point
`node --import tsx apps/cli/src/bin/corpus.ts --workspace <path>` against **real `corpus init`
workspaces and real spawned daemons** — no `node:http` stubs were used as E2E evidence.
Ports `8979` (wsA), `8980` (wsB), `8981` (wsC); scratch `/tmp/corpus-e008-c008-BZ9ZZH`.
`8765` was never bound.

## E2E Proof-of-Work Audit

| Check                                         | Result      | Notes                                                                                                                                                                                                                                                    |
| --------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Log present and per-item                      | PASS        | Verdict table for all five items, plus a section per item with transcripts.                                                                                                                                                                              |
| Commands specific (copy-pasteable, exit codes) | PASS        | Every claim carries the command, the output and `exit=N`.                                                                                                                                                                                                |
| Real E2E against real servers, not stubs      | PASS        | Item 1 used two real `corpus init` workspaces contending for one port with real spawned daemons, plus `curl /api/health` showing the `workspace` field. Items 2–4 ran against a real server. Stubs appear only in unit tests, which is what the sprint allows. |
| Covers the acceptance criteria                | PASS        | AC 1 (each item fixed-with-test or waived-with-rationale) and AC 2 (gate) are both addressed with evidence.                                                                                                                                               |
| App restarted after changes                   | PASS        | Separate "pre-fix reproduction" and "post-fix verification" sections; the post-fix run re-inits and re-starts real daemons.                                                                                                                               |
| `implemented on:` line                        | PASS        | `**implemented on: opus** (claude-opus-5, 1M context)` at the head of the log; also `implemented on: opus` in the commit message.                                                                                                                         |
| Reproduction-before-fix per FIXED item        | PARTIAL     | Item 1 (the only item that was a latent runtime bug) has a full pre-fix reproduction showing the lying pidfile and `exit=0`. Item 5 shows the rule catching a real violation. **Item 2 has no captured pre-fix transcript** — the old behavior is stated in prose from the hardcoded header, not demonstrated. Minor; see Minor Observations. |
| Self-reported gate claims                     | PASS        | `apps/cli` "51 files, 530 tests" re-derived exactly. Typecheck re-derived green. ESLint re-derived clean on all tracked TS. Generated-artifacts check re-derived green (the log's "reports stale until committed" explanation is now confirmed by the post-commit run). |
| Honest disclosure of gaps                     | PASS        | Explicitly states TEST-133's "deferred edit re-enters the queue" clause was not exercised and why; explicitly flags the port-band deviation; explicitly records a defect the E2E pass itself found (wrong port named in the foreign message) and its pinning test. |

## Log Honesty Re-derivation

Ten falsifiable claims picked from the log and re-derived independently. Every one **CONFIRMED**;
none contradicted.

| Claim in log                                                                                                                                | Re-derived?   | Actual observation                                                                                                                                                                                                                                        |
| ------------------------------------------------------------------------------------------------------------------------------------------ | ------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `server start` in the contending workspace: `corpus: :PORT is held by another workspace's server (…/wsA)`, `exit=4`, and **no pidfile**       | **CONFIRMED** | stdout empty; stderr `corpus: :8979 is held by another workspace's server (/tmp/corpus-e008-c008-BZ9ZZH/wsA)` + hint `Give this workspace its own port: change `port` in .corpus/config.json (or set CORPUS_PORT), then start again.`; `EXIT=4`; `ls -la wsB/.corpus/` shows `config.json`, `template-manifest.json`, `attachments/ jobs/ locks/ queue/` and **no `server.pid`**. |
| `server start --json` on the same contention emits one `{"error":…}` line on **stderr**, stdout empty, `exit=4`                              | **CONFIRMED** | stdout `[]`; stderr `{"error":{"code":"server_unreachable","message":":8979 is held by another workspace's server (/tmp/corpus-e008-c008-BZ9ZZH/wsA)"}}`; `EXIT=4`; still no pidfile.                                                                       |
| `server status` in the contending workspace reports not running, `exit=6`                                                                    | **CONFIRMED** | stdout `not running`; stderr `corpus: the workspace server is not running` + `Start it with `corpus server start`.`; `EXIT=6`.                                                                                                                             |
| The new `foreign` state: a live pid **and** a healthy answer reads as not-ours, `exit=6`, and `stop` leaves the live pid alone                | **CONFIRMED** | `pid 33392 is alive, and :8979 is held by another workspace's server (…/wsA)` / `EXIT=6`; `stop` → `not running (stale pidfile removed) — :8979 is held by another workspace's server (…/wsA), and pid 33392 was left alone`, exit 0; wsB's real server on `:8980` still `LISTEN`; wsA's `/api/health` still `{"status":"ok",…,"workspace":"…/wsA"}`. |
| `lock break <doc> --from agent` → exit 2, `breaking a lock is user-only — the agent waits, never breaks`, **nothing sent**                    | **CONFIRMED** | stdout empty; stderr exactly that line + `Wait for the holder and retry — a job blocked on a lock is deferred, not forced. Nothing was sent to the server. Check who holds it with `corpus lock list`.`; `EXIT=2`; `lock list` still shows the agent lock; commit count unchanged **2 → 2**. |
| `CORPUS_FROM=agent corpus lock break <doc>` → identical refusal, identical exit 2                                                            | **CONFIRMED** | Byte-identical stderr; `EXIT=2`.                                                                                                                                                                                                                          |
| The guard runs **before** the transport (a sent request would be exit 4)                                                                     | **CONFIRMED** | In a workspace with no server: `lock break … --from agent` → `EXIT=2` (usage error); `lock break …` (no override) → `EXIT=4` `corpus: server not running for this workspace — run `corpus server start``.                                                     |
| Breaking as user is audited in the commit message                                                                                            | **CONFIRMED** | `git -C wsA log`: `16035d6 user lock: force-break on doc_ntnw75vn (was agent) by user`, `2974033 user lock: force-break on doc_ntnw75vn (was agent) by user`.                                                                                              |
| Four tag edits produce **one** commit (server squash), tag list correct on disk after each                                                   | **CONFIRMED** | Disk `tags:` after each call: `[housing, finance]` → `[housing]` → `[housing, urgent]` → `[housing, urgent]`; commit count 4 → 5 (exactly one `doc edit:` commit).                                                                                          |
| The hygiene suite fails naming the file when a `process.stdin` read is introduced (`2 failed \| 8 passed`)                                    | **CONFIRMED** | Independently reproduced in a scratch copy: `expected [ 'queue/claim-all.ts' ] to deeply equal []` and `expected [ 'commands/queue/claim-all.ts' ] to deeply equal []`; `Tests 2 failed | 8 passed (10)`.                                                    |
| `apps/cli` is 51 files / 530 tests, all passing                                                                                              | **CONFIRMED** | `vitest run apps/cli` → `Test Files 51 passed (51)`, `Tests 530 passed (530)`.                                                                                                                                                                             |
| `readAll` has exactly one definition (`input.ts`) and `job/log.ts` imports it                                                                | **CONFIRMED** | `grep -rn "function readAll" apps/cli/src` → one hit, `apps/cli/src/input.ts:144`. `job/log.ts:2` imports `readAll, stdinCarriesABody, stdinStream` from `../../input.js`; no local definition.                                                            |
| `check-generated-artifacts.ts` goes green once committed                                                                                     | **CONFIRMED** | `✓ API contract is up to date` / `✓ CLI reference is up to date (docs/cli.md).` exit 0.                                                                                                                                                                    |

## Item Verdict Table

| Item                             | Issue-file verdict          | Evidence quality                                                                                                                                                  | My finding                                                                                                        |
| -------------------------------- | --------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| 1 — `probeHealth` ignores workspace | **FIXED**                  | Strong. Pre-fix reproduction with the lying pidfile, post-fix transcripts, five named tests, and a self-found second defect (wrong port named) pinned by its own test. | **Upheld.** Independently reproduced end to end on 8979/8980/8981. Exit code, message and pidfile-absence all exact. |
| 2 — `lock break --from agent`    | **FIXED** (behavior change) | Strong on the after-state; the before-state is asserted from the source, not captured.                                                                             | **Upheld.** Refusal, exit 2 and the nothing-sent half all re-derived. Behavior change is stated plainly in the log. |
| 3 — tag edit read-modify-write   | **WAIVED with rationale**   | Correct outcome and properly written: `edit.ts`'s header names the exact race, the verb's help publishes it, `docs/cli.md` carries it, two tests pin it.            | **Upheld — the waiver is the right verdict.** No conditional-write primitive exists; the rationale is written, not silent. |
| 4 — `readAll` duplication        | **VERIFIED CLOSED**         | Evidence quoted (single definition + import site) plus real-server exercise of all input routes.                                                                    | **Upheld.** Static evidence re-derived; both input routes and the socket case re-derived against a real server.     |
| 5 — stdin discipline             | **FIXED**                   | Strongest of the five: the shipped design is stated, the scan is exhaustive, and the rule is shown catching a real violation with real failing output.              | **Upheld.** Design re-derived by grep; violation independently reproduced in a scratch copy.                        |

## Criteria Results

| #        | Criterion                                                          | Result | Notes                                                                                                                                                                                                                                                                                                                                                                                                            |
| -------- | ------------------------------------------------------------------ | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| TEST-126 | Every one of the five has a verdict                                | PASS   | Verdict table rows 1–5: FIXED / FIXED / WAIVED-with-rationale / VERIFIED CLOSED / FIXED. Each FIXED names its regression tests; the WAIVED carries a written rationale. No silence.                                                                                                                                                                                                                              |
| TEST-127 | No new exit codes, no registry surprise                            | PASS   | Every exit code observed across the whole evaluation was `0`, `2`, `4` or `6` — inside `docs/cli.md`'s fixed set (0–6), which the CLI-008 diff does not touch. No flag was added at all (the diff adds no `flags:` entry), and the binary loads on every invocation, so `registry/validate.ts`'s module-load check passed. Help renders at all three levels: `corpus --help` → `corpus — conversations around documents, driven by an agent.` (0); `corpus server --help` → `corpus server — Manage this workspace's server process.` (0); `corpus lock break --help` → `corpus lock break — Force-unlock a document (an operator action).` (0). |
| TEST-128 | A foreign server on the port is not mistaken for this workspace's  | PASS   | wsA's daemon on `:8979` (pid 73919); wsB (a different directory) repointed to `:8979`. `corpus server status` in wsB: stdout `not running`, stderr `corpus: the workspace server is not running` / `Start it with `corpus server start`.`, **exit 6**. Also verified the harder live-pid variant: with wsB's own daemon alive on `:8980` and its config pointing at `:8979`, status reports `pid 33392 is alive, and :8979 is held by another workspace's server (/tmp/corpus-e008-c008-BZ9ZZH/wsA)`, exit 6 — never RUNNING. |
| TEST-129 | `start` does not write a pidfile for a dead child                  | PASS   | `corpus server start` in wsB: stdout empty, stderr `corpus: :8979 is held by another workspace's server (/tmp/corpus-e008-c008-BZ9ZZH/wsA)` + a remediation hint, **exit 4** (documented: "server unreachable"). `ls -la wsB/.corpus/` after the failure lists only `config.json`, `template-manifest.json` and the four runtime dirs — **`server.pid` absent**. The `--json` form behaves the same with the error as one line on stderr and stdout empty. The port is refused *before* anything is spawned, so no orphan child exists either. |
| TEST-130 | The happy path is unchanged                                        | PASS   | wsC on `:8981`: `start` → `corpus 0.0.0 listening on http://127.0.0.1:8981 (pid 32518)` exit 0; `status` → `running — pid 32518 on :8981, corpus 0.0.0, up 0s, …` exit 0; idempotent `start` → `already running on :8981 (pid 32518) — http://127.0.0.1:8981` **exit 0**; `stop` → `stopped (pid 32518)` exit 0; `status` after stop → exit **6** (state still gates the exit code). Stale pidfile: a bogus `{"pid":99998,…}` written after stop is detected and cleaned — `not running (stale pidfile removed)`, exit 6, and the file is gone afterwards. |
| TEST-131 | `--from agent` refused before any request is sent                  | PASS   | `lock break doc_ntnw75vn --from agent` → stdout empty; stderr `corpus: breaking a lock is user-only — the agent waits, never breaks` / `Wait for the holder and retry — a job blocked on a lock is deferred, not forced. Nothing was sent to the server. Check who holds it with `corpus lock list`.`; **exit 2**. Nothing-sent proven three ways: the agent's lock is still held (`lock list` → `doc_ntnw75vn — agent, acquired …, lease 300s`), the workspace commit count is unchanged (2 → 2, no audit entry), and in a server-less workspace the same command still exits 2 while the un-overridden form exits 4 — so the guard precedes the transport. |
| TEST-132 | `CORPUS_FROM=agent` refused identically                            | PASS   | `CORPUS_FROM=agent corpus lock break doc_ntnw75vn` → byte-identical stderr to TEST-131, **exit 2**. The env path is guarded, not just the flag (the guard tests `context.actor`, which `resolveActor` computes from both).                                                                                                                                                                                        |
| TEST-133 | Breaking as user still works and is still audited                  | PASS   | No override → `broke the agent lock on doc_ntnw75vn.` exit 0, then `lock list` → `no locks held.` Repeat call (404) → `no lock held on doc_ntnw75vn.` **exit 0**, nothing on stderr. `--from user --json` → `{"docId":"doc_ntnw75vn","broken":true,"holder":"agent"}` exit 0. Audit trail: `16035d6 user lock: force-break on doc_ntnw75vn (was agent) by user`. **The "deferred edit re-enters the queue" clause was not exercised** — it is server/agent-runtime behavior with no deferred edit in this scenario and nothing in CLI-008 touches it; the log discloses this explicitly rather than claiming it. Not held against the item. |
| TEST-134 | Stale module prose refreshed and docs regenerate                   | PASS   | `break.ts`'s header no longer says "the actor is overridden per call"; it now says "**It is user-only, and `--from agent` is refused rather than rewritten**" with the attribution rationale. `docs/cli.md`'s `lock break` paragraph regenerated to "**The agent may not run this.** `--from agent` (or `CORPUS_FROM=agent`) is refused here, before any request is sent, with exit 2…". `node --import tsx scripts/check-generated-artifacts.ts` → `✓ API contract is up to date` / `✓ CLI reference is up to date (docs/cli.md).`, exit 0. |
| TEST-135 | The race is documented because there is nothing to mitigate with   | PASS   | Verdict is **WAIVED-with-rationale**, which is the correct outcome, and it is written rather than implied. `edit.ts`'s module header states: "Two concurrent `--add-tag` calls can each `GET` the same list and each `PUT` its own merge, and the second write wins with the first one's tag missing", names the absence of `If-Match`/ETag/version in `packages/contract` and `apps/server`, explains why per-edit locking is the wrong trade, and points to a CONTRACT issue as the real fix. The issue file records the same, and the log escalates it as worth filing. The hazard is also published to users via the verb's help and `docs/cli.md`. |
| TEST-136 | `--add-tag` / `--remove-tag` still behave                          | PASS   | Read off disk after each call: add two → `tags: [housing, finance]`; remove one → `[housing]`; add-and-remove-and-add in **one** call (`--add-tag draft --remove-tag draft --add-tag urgent`) → `[housing, urgent]` (`draft` correctly absent); remove-when-absent → `[housing, urgent]` unchanged, exit 0. Commits: 4 → 5 — the four edits fold into a single `doc edit:` commit under the server's 30 s same-doc/same-actor squash window; no call produced more than one commit. |
| TEST-137 | Verified closed, with evidence                                     | PASS   | `grep -rn "function readAll" apps/cli/src` → exactly one hit: `apps/cli/src/input.ts:144: export async function readAll(...)`. `apps/cli/src/commands/job/log.ts:2` imports `{ readAll, stdinCarriesABody, stdinStream, type InputDependencies }` from `../../input.js` and defines none of them. Behaviorally against a real server: argument form → `{"eventId":"evt_pdlytsbb5chm","appended":true}` exit 0; piped form → same, exit 0; heredoc → same, exit 0; all three lines present in `.corpus/jobs/evt_pdlytsbb5chm.jsonl`. The socket case (this harness hands the child a socket on fd 0) refuses instead of hanging: `corpus: no line to append.` exit 2. CLI-007's regression test passes by name: `apps/cli/src/input.test.ts -t "accepts a heredoc"` → `Tests 1 passed | 19 skipped`. |
| TEST-138 | Enforcement covers every command module, not three topics          | PASS   | `hygiene.test.ts`'s `commandModules()` reads `import.meta.dirname` **recursively**, so scope is "every `.ts` under `commands/`" by construction, not a topic list. The "finds the modules it is supposed to be guarding" assertion for the stdin rule ("scans every command module, not a chosen few") asserts the **exhaustive 37-entry list** — `db/`(3), `doc/`(6), `health.ts`, `init/`(5), `job/`(3), `lock/`(3), `queue/`(6), `server/`(7), `thread/`(3) — so a new verb or a new topic directory shows up as a failing diff rather than as an unguarded module. Suite runs green: `Tests 10 passed (10)`. The old `TOPICS = ["doc","thread","db"]` list survives only as `WRITE_RESTRICTED_TOPICS`, which is the *write* rule and correctly scoped (SPEC.md §2.2 rule 4 lets `server/` and `init/` write the pidfile, the log and the scaffold). |
| TEST-139 | The rule permits nothing it should not, forbids nothing it must    | PASS   | **The absolute ban shipped, achieved by moving the call sites** — Open Conflict 10's recommended design, not a ban with carve-outs. `grep -rn "process\.stdin" apps/cli/src` (non-test) returns hits in **`input.ts` only** (`:111` `return process.stdin;`, `:115` `process.stdin.isTTY === true`, plus prose at `:101/:107/:120`) and one prose mention in `testing/stdin.ts:4` that `stripProse` removes before the rule sees it. `job/log.ts:43` now reads `dependencies.stdin ?? stdinStream()` and `doc/delete.ts` moved onto the same accessors. The single constant is `STDIN_OWNER = "input.ts"` — one owner, **no exemption list**. The log states which design shipped and why, verbatim: "the two legitimate call sites (`job/log.ts`, `doc/delete.ts`) moved onto them, so the rule is absolute with **no per-file exemptions** (Open Conflict 10's recommended design)", and the test's own header says "an exemption list is how the next module talks its way onto the exception". |
| TEST-140 | The rule catches a real violation and the socket-hang class stays closed | PASS   | **DEMONSTRATED independently, not asserted.** `apps/cli` was copied to `/tmp/corpus-e008-c008-BZ9ZZH/clicopy` (the corpus working tree was never modified), the suite was confirmed green there (`Tests 10 passed (10)`), then `export const rogue = process.stdin.isTTY;` was appended to `commands/queue/claim-all.ts`: `Tests 2 failed | 8 passed (10)` with `AssertionError: expected [ 'queue/claim-all.ts' ] to deeply equal []` and `expected [ 'commands/queue/claim-all.ts' ] to deeply equal []` — **the message names the file**. Protected behavior still holds: `job log` from an argument, a pipe and a heredoc all succeed against a real server, the non-TTY socket case exits 2 instead of hanging, and `echo "yes" \| corpus doc delete doc_ntnw75vn` (no `--yes`, non-TTY stdin) → stdout empty, stderr `corpus: refusing to delete doc_ntnw75vn without --yes.` / `Deleting is irreversible in the working tree (git keeps the history). Re-run with --yes, or archive instead.`, **exit 2**, with the document still on disk — the piped `yes` was not consumed as a confirmation. `doc delete --from agent --yes` still refuses with `corpus: deletion is user-only — the agent archives, never deletes`, exit 2. |

## Minor Observations

None of these is a criterion failure; all are recorded for the record.

1. **Stale `thread resolve --json` help prose** (pre-adjudicated as a MINOR, carried forward from
   CONTRACT-007, not a CLI-008 regression). The verb now emits `{thread, warnings}` per
   CONTRACT-007 — re-derived live:
   `{"thread":{"id":"th_m3slsbtj",…,"status":"resolved",…},"warnings":[]}`, exit 0 — but the help
   example still reads `# One JSON value: the thread summary, with `status` now `resolved`.`
   (`corpus thread resolve --help`, example line). `docs/cli.md`'s generated-artifact check is
   green, so this is prose drift inside the (correct) generated text, not artifact staleness.
   **Classified as a minor observation with a pointer, not a criterion failure**, and it belongs to
   the contract/thread-resolve surface rather than to CLI-008.

2. **Item 2 has no captured pre-fix transcript.** Item 1's reproduction is exemplary; item 2's
   "before" state is asserted from the hardcoded `header: {"x-corpus-author":"user"}` (which the
   diff confirms was there) and stated plainly as a shipped behavior change, but no
   `exit=0`-with-wrong-attribution transcript was captured before the guard landed. The behavior
   change itself is well flagged ("Item 2 — the behavior change, stated plainly"), which is the
   part that matters for a reviewer. Worth a habit note, not a re-run.

3. **Port-band deviation, disclosed.** The implementation log ran on `8920`–`8924`, inside
   SERVER-014's sprint-008 band, rather than CLI-008's allocated `8951`–`8959`. The log discloses
   this at the top, attributes it to an orchestrator instruction, and records `lsof` checks before
   and after. No collision was observed. Process nit only.

4. **TEST-133's queue clause remains unexercised** (disclosed by the log). If the orchestrator
   wants that clause closed, it belongs to a server/agent-runtime issue, not to CLI-008.

5. **A transient repo-level lint/format failure was observed mid-evaluation and was NOT CLI-008.**
   During this evaluation `npm run lint` and `npm run format:check` were briefly red, entirely
   because of a sibling evaluator's untracked scratch directory `.eval-scratch-e008/`
   (`loop.mjs`…`probe.mjs`; `'process' is not defined`, `'document' is not defined`, Prettier
   style). ESLint over the tracked sources exited 0 throughout. The sibling has since removed the
   directory and the gate is now green end to end, re-derived: `npm run lint` exit **0**,
   `npm run format:check` → `All matched files use Prettier code style!` exit **0**,
   `npm run typecheck` exit **0**. Recorded only so the transient red is not later misattributed.

## Failures

None. All 15 criteria pass.

## Summary

CLI-008 passes on all 15 sprint-008 criteria. The two items with real behavioral surface — item 1
(foreign-server detection in the lifecycle verbs) and item 2 (`lock break` refusing an agent actor)
— were both reproduced end to end against real workspaces and real daemons, and the observed exit
codes, stderr strings and pidfile state match the log character for character. The waiver on item 3
is the correct outcome and, critically, is *written* — the accepted read-modify-write race is named
in the module header, in the verb's help and in `docs/cli.md`, with the reason no mitigation exists
and what would be required to close it. Item 4 closes as verified with quoted evidence and a
re-derived real-server exercise of all three input routes plus the socket case. Item 5 shipped the
stricter of the two designs Open Conflict 10 offered — an absolute ban achieved by relocating the
two legitimate call sites onto `input.ts` accessors, with a single owner and no exemption list —
and its enforcement was **demonstrated independently in a scratch copy**, failing with a message
that names the offending file, without touching the corpus working tree.

The log is honest: it names a defect its own E2E pass found and pinned, it names the one criterion
clause it did not exercise and why, and it names its own port-band deviation. Nothing it claimed
was contradicted by re-derivation.

**Environment left clean**: ports `8979`, `8980`, `8981` free; `8765` unbound and never bound;
all scratch servers stopped (wsA and wsC via `corpus server stop`, wsB's surviving daemon killed by
recorded pid 33392); no tracked file in the corpus repo was modified by this evaluation.
