# [SHARED-001] Revise SPEC.md for the standalone-tool architecture

## Domain
shared (orchestrator-handled, with spec-writer)

## Status
done — verified 2026-08-13 (INFRA-027) against commit `f805267d`, which carries this
id. The work landed; this file was never ticked.

## Priority
P0

## Model
fable — full-spec architectural revision; every downstream issue inherits its judgment calls.

## Dependencies
- Depends on: —
- Blocks: CONTRACT-001, all Phase 1 decomposition

## Spec References
- SPEC.md §2 (architecture), §3 (tech stack), §4 (repository layout), §7 (event queue), §9 (server/write paths), §11 (hooks), §12 (milestones)

## Summary
SPEC.md describes a repo-as-app system where the CLI writes files directly and the server watches. The project has pivoted (decisions recorded in CLAUDE.md, 2026-07-26): Corpus ships as an installable CLI tool, the server is the sole writer, the agent interacts only through the CLI, and the API is contract-first via `@hono/zod-openapi` with a generated client shared by UI and CLI. Revise SPEC.md so it is again the single source of truth, with no contradiction between spec and decisions.

## Acceptance Criteria
- [x] §2/§3/§4 describe the tool/workspace split: installed tool (npm bin `corpus`; server + CLI + statically served pre-built UI) vs. `corpus init`-created workspace (`data/`, `.corpus/`, config, git, product agent skills).
- [x] All write paths route through the server; CLI is specified as a thin typed-client wrapper with no direct file access; "files are source of truth" and git auto-commit (author attribution, audit trail) are preserved server-side; watcher's role reduced to out-of-band edit reconciliation.
- [x] Queue section rewritten: `corpus queue idle` long-polls the server (same rearm semantics); claim/complete/fail/halt are API calls.
- [x] New contract section: `packages/contract` zod-openapi route definitions, committed generated `openapi.json` with drift check, generated typed client consumed by UI and CLI.
- [x] Tech stack updated: TS everywhere (CLI included — replaces "plain Node ESM .mjs"), Vitest replaces node test runner, monorepo layout matches CLAUDE.md's Repository Structure.
- [x] Auth: localhost bind + bearer token generated by `corpus init`.
- [x] Server lifecycle commands specified: `corpus server start|stop|status|logs` (daemonized, pidfile in `.corpus/`), idempotent start, CLI errors clearly when server is down.
- [x] CLI self-documentation specified: a single declarative command registry (name, args, flags, description, examples) drives the dispatcher, all `--help` output, and a generated committed `docs/cli.md` with a pre-push/CI drift check — the CLI mirror of the OpenAPI approach (CONTRACT-001 specifies the same for `openapi.json`).
- [x] §12 milestones re-cut to match the new build order (contract → server → cli → ui → agent loop → plugins), each with an executable check.
- [x] No section of SPEC.md contradicts CLAUDE.md's Architecture Decisions; the ⚠️ override note in CLAUDE.md can be removed after this lands.

## Technical Design

### Files to Create/Modify
- `SPEC.md` — revise in place (git history preserves the prior version)
- `CLAUDE.md` — remove the ⚠️ spec-override note once the spec is consistent

### Key Implementation Details
Work with the spec-writer agent: behavioral spec stays behavioral (WHAT), architecture sections get the new topology. Keep proven mechanisms unchanged where the pivot doesn't touch them: document model (§5), threads/anchors (§6), participation semantics (§8), UI board (§10), plugins (§10, but plugin server routes/CLI verbs now register through the tool's server/CLI), publish plugin (§13). Preserve the queue's zero-token parking semantics and the ~250 ms round-trip target.

### Edge Cases
- Out-of-band file edits (external editor) still must be detected, reconciled (anchors), and projected — spec must say the watcher survives as reconciliation/projection input, not as the CLI's write channel.
- Multiple workspaces on one machine: port/token per workspace config; `corpus` commands resolve the workspace from cwd.

## Testing Strategy
N/A (documentation). Review pass: grep the revised spec for "cli/lib", "fs.watch", ".mjs", "node test runner" — none should survive except in a changelog note.

## E2E Verification Plan
N/A (documentation issue). Verification = acceptance criteria review by orchestrator + user sign-off.

### Verification Steps
1. Read revised SPEC.md end-to-end against the acceptance criteria.
2. Confirm CLAUDE.md and SPEC.md agree on every architecture decision.

## E2E Verification Log

Documentation issue — review-based verification. **Implemented on: fable** (spec-writer agent, 2026-07-26).

**Grep review (Testing Strategy):** `grep -n "cli/lib\|fs.watch\|\.mjs\|node test runner" SPEC.md` → **0 matches** (the terms do not survive anywhere, including the revision note, which describes the pivot without quoting pre-pivot mechanics). `npx prettier --check SPEC.md CLAUDE.md` → all files formatted correctly.

**Criteria review (section map):**
- Tool/workspace split: §2.1 (install vs. `corpus init`, workspace resolution by cwd, per-workspace port/token), §3 (monorepo matching CLAUDE.md's Repository Structure incl. `assets/workspace/` shipping inside the package), §4 (user-workspace layout only; tool repo explicitly delegated to CLAUDE.md).
- Server as sole writer / thin CLI: §2.2 rules 1 and 4 (rule 3 kept in position — SSE invalidations only, as cited by SERVER-009); §6/§7/§9 write-path language moved to the server; watcher rescoped to out-of-band reconciliation + job-log tailing (§2.2 rule 1, §9.1).
- Queue over HTTP: §7 verbs rewritten as API calls; long-poll idle with ~8 min rearm; zero-token parking and ~250 ms round-trip preserved verbatim; §9.2 gains queue/locks/health endpoints.
- Contract-first: new §9.3 (numbered so existing issue references to §9.1/§9.2 stay valid); committed `openapi.json` + drift check + typed client consumed by UI and CLI.
- Auth: §2.1 (localhost bind, bearer token from init, mode-protected config, SSE token param, health + loopback job-log ingest exceptions).
- Lifecycle verbs: §2.1 (`start|stop|status|logs`, daemon + pidfile/logfile in `.corpus/`, idempotent start, stale-pid handling, "run `corpus server start`" error surface).
- CLI self-documentation: new §2.3 (declarative registry → dispatcher + three-level `--help` + generated committed `docs/cli.md` + pre-push/CI drift check); §11 lists both artifact drift checks.
- Milestones: §12 re-cut to six milestones (contract → server → CLI → UI → agent loop → plugins), each with an executable check; v1 DoD is install-from-pack → init → start.
- CLAUDE.md: ⚠️ override note removed; Architecture Decisions section left intact. Read both end-to-end — no remaining contradiction found.

**Decomposition-pinned decisions arbitrated (all adopted, none overturned):** (1) job-log streaming = SSE invalidate + HTTP refetch (§2.2 rule 3, §7, §10 console); (2) autosave squash = amend-within-idle-window, pinned behaviorally in §4 ("one commit per editing session"); (3) workspace template ships inside the installed package (§2.1, §3); (4) §4 describes the user workspace, distinct from the tool monorepo.

## Completion Checklist (domain agent)
- [x] Acceptance criteria verified

## Completion Checklist (orchestrator)
- [x] User sign-off on the revised spec (PR #6 merge, 2026-07-26)
- [x] Committed with `[SHARED-001]` prefix (landed in #6, squash f805267)
