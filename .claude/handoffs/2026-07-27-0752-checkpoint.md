> **Correction (orchestrator, 2026-07-27, minutes after this checkpoint):** the snapshot below was taken mid-flight and three of its flags are already resolved: (1) the sprint-006 evaluator DID finish — `issues/evals/CLI-004-eval.md` exists, verdict **PASS** for both SERVER-006 and CLI-004 (CONTRACT-006's surface verified within it); SERVER-006/CLI-004/CONTRACT-006 are flipped to done in PLAN.md and their issue files. (2) DISC-1's log line is corrected in issues/server/006-thread-write-paths.md. (3) The SERVER-017 evaluator is a separate agent still running on ports 8960-8969 — do not re-run it; its verdict flips SERVER-017 to done. (4) SERVER-017's PLAN row now lives in the Phase 2 table. Remaining Phase 2 work: SERVER-010 + CLI-003 (sprint-007), then cut the Phase 2 PR.

# Checkpoint: 2026-07-27 07:52

Phase 2 (`phase-2-server-cli`) is in its tail. See `issues/PLAN.md` for per-issue status; this
file carries only what the plan and issue files do not.

## State Summary

- **Branch**: `phase-2-server-cli`, tip **39f5496** (`[SERVER-017]` Domain-Knowledge bookkeeping)
  over **c7d9d73** (`[SERVER-017]` db rebuild/doctor routes). **54 commits ahead of `main`**
  (the "~62" figure in the session summary was wrong — verified with `git rev-list --count`).
  **No upstream configured — the branch has never been pushed.**
- **Working tree**: clean except one untracked file, `issues/evals/SERVER-006-eval.md` (below).
- **Done this phase**: CONTRACT-002/003/004/005, SERVER-003/004/005/007/008/009/011/012/013/015,
  CLI-001/002.
- **In progress (PLAN.md)**: SERVER-006, CLI-004, CONTRACT-006, SERVER-017.
- **Remaining Phase 2 after evals clear**: SERVER-010 (attachments; dep SERVER-006) and
  CLI-003 (doc/thread verbs; deps SERVER-006 + SERVER-017) — the final batch.
- **Deferred**: SERVER-014 (P2 duplicate-survivor policy) → Phase 3. CONTRACT-007 + SERVER-016
  are already scheduled in Phase 3 ahead of UI-008.

## Evaluation State — read this before assuming anything is done

Two evaluators were running in the background at checkpoint time. **Neither environment is
alive now**: no `corpus`/`tsx apps/server` processes remain, `.claude/worktrees/` does not
exist. Only one verdict reached disk.

- **SERVER-006 — PASS** (`issues/evals/SERVER-006-eval.md`, untracked, dated 2026-07-27).
  75/76 criteria PASS, 1 PARTIAL. The PARTIAL (**DISC-1**) is documentation-only: SERVER-006's
  E2E Verification Log claims an unencoded `:` in the turn-delete path returns 404; the shipped
  router actually returns **200 and deletes the turn** (malformed values still 400). **Required
  before the issue closes**: correct that line in `issues/server/006-thread-write-paths.md`'s
  E2E log to state the shipped behaviour, or file it as a follow-up. Two non-blocking
  observations in that verdict are worth carrying to UI work: thread _deletion_ emits no
  `["tree"]` SSE key while creation does (flag for UI-008), and `GET /api/jobs?recent=5`
  returns `originTitle: null` for a thread-origin job (flag for UI-011).
- **CLI-004 and CONTRACT-006 — no verdict on disk.** The sprint-006 evaluator wrote a
  SERVER-006-only file; whether it died before covering the other two or was scoped that way
  is unknown. Treat both as **unevaluated**.
- **SERVER-017 — no verdict on disk.** Its solo eval (ports 8960-8969) left scratch behind
  (`/tmp/corpus-e017-claim.json`) but produced no file. Unevaluated.

The eleven-step agent-loop E2E (TEST-121…132) in `issues/sprints/sprint-006.md` is the sprint's
centrepiece and the one thing that proves the loop closes end to end — do not let it be skipped
in a re-run.

## Session Context (not in any persistent file)

- **Phase 2 lands as ONE squash PR** after all its issues pass evaluation. The diff contains
  **no SPEC.md changes**, so the merge is autonomous — no user sign-off hold.
- **API instability pattern this phase**: subagents dying mid-response. Remedy that worked —
  `SendMessage` to resume when progress was already banked; a _fresh_ agent pointed at the
  existing worktree with take-stock-first instructions when transcript replay kept dying.
- **Gate baseline to beat**: 183 test files / 3113 tests; coverage 98.73% lines / 94.95%
  branches / 99.37% functions. Full gate = `npm run build` → `lint` → `format:check` →
  `typecheck` → `test:coverage`.
- **Prettier every edited `.md` before committing** (`npx prettier --write`) — pre-commit
  blocks otherwise. This handoff has been formatted.
- Durable mechanics are already recorded, do not re-derive: memory
  `~/.claude/projects/-Users-theophanerupin-code-corpus/memory/corpus-orchestration-gotchas.md`
  (worktree harvest via `git apply -3`, 8765 reserved for UI e2e, `CORPUS_UI_PORT=5273`,
  `node --import tsx` not `npx`, pid-only kills, per-agent scratch prefixes);
  `.claude/agents/server-dev.md` Domain Knowledge (six anchor-engine adjudications + the
  reopen-seam entry); `.claude/agents/cli-dev.md` (registry rules).

## File Changes Since the Last Checkpoint

- `packages/contract/**` — thread-response warnings, `appended` honesty, db routes (CONTRACT-006)
- `apps/server/**` — thread write paths incl. creation/turns/events/cascade (SERVER-006);
  capped job-log appends answering `appended:false` (SERVER-009); db rebuild/doctor routes
  mounted with the reopen seam (SERVER-017)
- `apps/cli/**` — queue/lock/job verbs, the agent-loop surface (CLI-004)
- `issues/sprints/sprint-006.md`, `issues/PLAN.md`, `.claude/agents/server-dev.md` — sprint
  contract, five conflict adjudications, CONTRACT-006/007 + SERVER-016/017 filed, reopen-seam
  Domain Knowledge

## Next Steps

1. **Re-run the missing evaluations**: CLI-004 + CONTRACT-006 against `issues/sprints/sprint-006.md`
   (agent-loop E2E mandatory), and SERVER-017 solo. Fresh workspaces, reserved ports.
2. **Fix SERVER-006 DISC-1** — the E2E log line about the unencoded `:`; then flip SERVER-006 to
   `done` in the issue file and PLAN.md, and commit `issues/evals/SERVER-006-eval.md` (currently
   untracked) with it.
3. **Final Phase 2 batch**: SERVER-010 + CLI-003 once SERVER-006/017 are done — sprint-planner
   for sprint-007 first (batch > 1).
4. **Cut the Phase 2 PR**: push `phase-2-server-cli` (verify with `git ls-remote`), `/pr`, spawn
   **pr-reviewer** fresh on fable, wait for `CI / validate` green, `gh pr merge --squash`.

## Inconsistencies for the Orchestrator

- **SERVER-017 is filed under the Phase 3 table in `issues/PLAN.md`** but is implemented and
  committed on `phase-2-server-cli`, and CLI-003 (Phase 2) depends on it. It will ship inside the
  Phase 2 PR regardless — decide whether to move its row into the Phase 2 table so the PR's scope
  and the plan agree.
- `.claude/handoffs/` now holds **4** checkpoints. Once this one is consumed, the two oldest
  (`2026-07-26-1323`, `2026-07-26-1814`) can be deleted.
