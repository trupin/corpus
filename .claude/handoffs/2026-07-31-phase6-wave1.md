# Checkpoint: 2026-07-31 — Phase 6 wave 1 in flight

Verified against the repo (read-only; no git commands run — three implementation
agents were live at write time). Plan state is in `issues/PLAN.md`; sprint terms are
in `issues/sprints/sprint-018.md`. Only what those files do **not** carry is below.

## State Summary

- **Landed**: PR #13 (UI-022 / UI-023 / UI-024 — dogfood polish) squash-merged to
  `main` at `a93316e`, 2026-07-31. Three user reports fixed; evaluator PASS ×3;
  pr-reviewer REQUEST_CHANGES → fixed → APPROVE; CI green.
- **In progress** (branch `phase-6-dogfood`, cut from `a93316e`), wave 1:
  - **SERVER-033** — server-dev (@hono/node-server v2 migration)
  - **UI-020 + UI-021** — ui-dev (unarchive affordance; form-turn renderer divergence)
  - **CLI-018** — cli-dev (agent-writable view keys)
- **Wave 2 queue**: CONTRACT-025 → SERVER-038 · UI-016 · UI-027 (P1, anchor
  highlights never render) · UI-028 (↵ activation) · then **SHARED-003** triage
  (orchestrator-side, no code).
- **Retrieval track**: 19 issues filed and confirmed present on disk
  (CONTRACT-022/023/024 + 025, SERVER-040–047, CLI-019–021, AGENT-008/009,
  UI-025/026, INFRA-012), spread across plan Phases 7/8/9. All `todo`.

## Session Context (not in persistent files)

- **PR #13 review trail** (nowhere in the issue files): pr-reviewer returned
  REQUEST_CHANGES with **1 MAJOR** — UI-024's decline path contradicted the _signed_
  SPEC §10 "Copy always" rule — plus **2 MINOR** (multi-block `intersectsNode`
  handling; stale-selection fingerprint). All three fixed, re-reviewed, APPROVE.
  If UI-024's behavior is revisited, the "Copy always" constraint is the binding one.
- **Wave-1 scope was corrected after the issues were written.** Read
  `sprint-018.md` before touching these — the issue files alone are stale:
  - **SERVER-033**: sprint-018 rewrote the hazard model — `c.env.incoming` guards,
    real-listener (not `app.request`) tests, package-manifest pin. Adjudication 4:
    a passing suite is _not_ evidence here.
  - **UI-020/021**: Archive moves onto a POST route (Adjudication 7); a **named
    kit exception** to the standing no-kit-change rule (Adjudication 6);
    Adjudication 8 bounds the docblock cleanup.
  - **CLI-018**: no contract change; the evidence chain is TEST-633/634 (SSE-driven,
    live board, no reload).
- **Adjudication 3 (serialization)**: SERVER-033 and UI-016 each bump a dependency
  major and must **never run concurrently** — that is why UI-016 sits in wave 2
  rather than wave 1. Preserve that ordering.
- **SHARED-006 is signed but deliberately unapplied.** 13 spec edits, user sign-off
  recorded 2026-07-30 ("Approve all 13"). They are applied to `SPEC.md` **only** as
  the first commit of the future retrieval branch — not on `phase-6-dogfood`.
  User decisions attached: local-first embedder (zero-key local tool); retrieval
  starts after the Phase 6 backlog.
- **SHARED-003's ledger grew** (`## Polish-eval ledger additions`, 2026-07-30):
  two findings were promoted to issues rather than ledgered (UI-027, UI-028), and a
  **§11 doctor-warnings rider is queued for the next spec sign-off round** — that
  rider is CONTRACT-025's premise, so CONTRACT-025 should not land ahead of it.
- **Plan-table drift, cosmetic**: wave-1 issues are physically rowed in the Phase 4
  table (SERVER-033, UI-016) and the Phase 5 table (SERVER-038, UI-020, UI-021,
  CLI-018), not in the Phase 6 table. The Phase 6 header prose says so explicitly.
  Do not "fix" this mid-flight; status updates go in the rows where they already are.

## File Changes This Session

- `issues/shared/003-pr11-review-followups.md` — gained the polish-eval ledger
  section (UI-022/023/024 eval fallout, §11 rider).
- `issues/evals/UI-022-eval.md`, `UI-023-eval.md`, `UI-024-eval.md` — PASS verdicts.
- `issues/PLAN.md` — Phase 6 table + Phases 7/8/9 (retrieval) added; UI-022/023/024
  marked done.
- 19 retrieval issue files added across `issues/{contract,server,cli,agent-runtime,ui,infra}/`.
- `issues/sprints/sprint-018.md` — wave-1 contract (TEST-581–656).

## Standing Constraints (carry forward verbatim)

- **Never touch port 8765** — the maintainer's live server; its pid respawns.
  Sprint-018 allocates `8792`–`8801` and Vite `5275`–`5279`; `8790`/`5273` belong to
  the pre-push hook.
- **Scratch root is outside the repo**: `/Users/theophanerupin/.claude/jobs/4dd0ddef/tmp`,
  per-issue `s018-<domain>/<nnn>-XXXXXX`. Never scaffold a workspace into the dev repo
  (TEST-651/652).
- `/usr/bin/grep` for negative evidence (the shell aliases `grep` to an rtk proxy).
- Commits are **orchestrator-only**, `[ISSUE-ID]`-prefixed, full gate per commit,
  backgrounded. Domain agents run scoped tests only.
- Merges are **squash-only**, on pr-reviewer APPROVE + green CI.
- SPEC changes require user sign-off. **No npm publish, ever.**

## Next Steps

1. Harvest wave 1 as each agent reports: scoped verification → repo-wide gate once →
   per-issue commit on `phase-6-dogfood`.
2. Sequence wave 2 respecting Adjudication 3 (UI-016 alone, never beside SERVER-033)
   and CONTRACT-025 → SERVER-038.
3. Prioritize **UI-027** (P1 — anchor highlights never render; a §10 violation in a
   shipped surface the user is dogfooding) ahead of the P2 remainder.
4. Run SHARED-003 triage yourself once wave 2 is filed; roll the §11 doctor-warnings
   rider into the next spec sign-off round.
5. Phase 6 PR → babysit to merge → then retrieval kickoff, whose **first commit**
   applies SHARED-006's 13 signed edits to `SPEC.md`.

## Housekeeping

`.claude/handoffs/` now holds 5 files. The two 2026-07-27 checkpoints are superseded
and can be deleted; keep `2026-07-29-phase4-eval-restart.md`,
`2026-07-30-phase5-checkpoint.md`, and this one.
