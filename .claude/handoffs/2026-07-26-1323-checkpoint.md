# Checkpoint: 2026-07-26 13:23

First handoff in `.claude/handoffs/` (dir was empty). See `issues/PLAN.md` for the
authoritative issue table — not repeated here.

## State Summary

- **Done (merged to `main`)**: Phase 0 complete — INFRA-001/002/003/005/006,
  SHARED-001 (spec revision, PR #6), CONTRACT-001 (contract bootstrap, PR #7).
  Both squash-merged. INFRA-007 also `done`.
- **In Progress**: SERVER-001, SERVER-002, UI-001, AGENT-001 — all four implemented
  and committed on branch `phase-1-foundations` (7 commits ahead of `main`, working
  tree clean, **not yet pushed to origin**). Statuses still say `in_progress` in both
  the issue files and PLAN.md, pending the evaluator verdict.
- **Evaluator**: running at checkpoint time. `issues/evals/` is still empty — no
  verdict file has landed yet.
- **Next Ready (Phase 2, once Phase 1 lands)**: CONTRACT-002 and SERVER-003 (both
  depend only on CONTRACT-001), CLI-001 (CONTRACT-001 + INFRA-007). Everything else
  in Phase 2 chains off those. INFRA-004 becomes ready once UI-001 is `done`.
- **Blocked**: nothing blocked on an unresolved question.

## Branch contents (`phase-1-foundations`)

```
0515dc0 [SERVER-001][SERVER-002] TEST-62: checker composes with the real anchor engine
2e5ba6a [UI-001] App scaffold + design system from design/index.html
ac6949f [SERVER-001] Document model core: parse/serialize, ids, validation
136440f [SERVER-002] Anchor engine: text-quote resolution + reconciliation
3c68fb8 [AGENT-001] Workspace template: skills layout, seed views, templates
8a78357 [PLAN] Sprint 001 contract for Phase 1 foundations batch
f16b004 [PLAN] Phase 1 batch in progress
```

111 files, ~11.3k insertions. **No `SPEC.md` change in this diff** (verified) — so
the Phase 1 PR does not need the user spec sign-off gate.

## Session Context (ephemeral — not in any issue file)

- **Harvest procedure for worktree-isolated agents** (domain agents never commit; the
  orchestrator harvests): `git apply -3` the worktree patch, **exclude
  `package-lock.json`** from the patch, run `npm install` to regenerate it, resolve
  union conflicts by hand. Used for all four Phase 1 issues.
- **TEST-62** (cross-issue integration: the SERVER-001 checker composing with the real
  SERVER-002 anchor engine) was verified by the orchestrator after harvest and landed
  as its own commit, not inside either issue's commit.
- **Model actuals are incomplete.** UI-001 records `implemented on: opus`;
  **SERVER-001, SERVER-002, and AGENT-001 have no `implemented on:` line in their E2E
  Verification Logs.** SERVER-002 was run on fable (its Model recommendation),
  SERVER-001/AGENT-001 on opus. Fill these in before marking done — the audit trail
  for recalibrating recommendations depends on it.
- **`.claude/agents/server-dev.md` and `cli-dev.md` picked up Domain Knowledge edits**
  in this branch; they are part of the Phase 1 diff.

## Standing Gotchas (environment, not project state)

- `rtk` proxies shell commands — use `rtk proxy <cmd>` when raw/unfiltered output
  matters (e.g. `git diff --name-only`).
- `git push` with `-u`/upstream does not stick here. Always
  `git push origin <branch>` and then verify `git ls-remote origin <branch>` tip
  equals local `HEAD`.
- Port 5173 is held by a developer ssh tunnel — run e2e with `CORPUS_UI_PORT=5273`.
- Every exported module needs a same-package test (`dist/` resolution + the coverage
  gate); build before lint/typecheck/test.
- Node 25 quirks: EventSource needs its flag; jsdom `localStorage` misbehaves.

## Cross-domain decisions recorded elsewhere (do not re-litigate)

- `issues/contract/002-contract-full-surface.md` — tri-state `requestsAgent`,
  `QueueStatus` as a superset, folder-default inbox, optional actor header.
- `issues/cli/002-init-server-lifecycle.md` — queue `.gitkeep` handoff from AGENT-001.
- `issues/sprints/sprint-001.md` — "Open Conflicts" section holds the adjudications
  made during this batch (e.g. the `doc_seed_attention` form correction). Also the
  contract-vs-server schema ownership rule: if a shape must exist in both places it is
  a CONTRACT-002 issue, never a second declaration.

## File Changes This Session

- `apps/server/**` (39 files) — document model core + anchor engine (SERVER-001/002)
- `apps/ui/**` (39 files) — Vite/Router/Query app shell, design-system transcription
  from `design/index.html`, Playwright smoke spec (UI-001)
- `assets/workspace/**` (11 files) — workspace template: skill skeletons, three seed
  pinned views, note template, gitignore (AGENT-001)
- `packages/kit/src/tokens.css`, `tokens.test.ts`, `index.ts` — design tokens surfaced
  through the kit (UI-001)
- `scripts/workspace-template.ts` + `.test.ts`, `docs/workspace-template.md` — the
  copy contract (rename table, filter list, generate-don't-copy list) that CLI-002 will
  consume; the test makes doc and code inseparable (TEST-58)
- `issues/sprints/sprint-001.md` — sprint contract, 64 numbered TEST-* criteria
- `vitest.config.ts`, `package.json`, `scripts/tsconfig.json`, `.prettierignore` —
  wiring for the new workspaces/scripts

## Next Steps

1. **Await the evaluator verdict** in `issues/evals/`. If FAIL, route it back to the
   owning domain agent (fix-loop, max 3).
2. On PASS: add the missing `implemented on:` lines to SERVER-001, SERVER-002,
   AGENT-001; flip all four to `done` in the issue files **and** `issues/PLAN.md`;
   commit as `[PLAN]` bookkeeping on the branch.
3. `git push origin phase-1-foundations`, verify with `git ls-remote`, then `/pr`.
4. **Babysit the PR**: spawn **pr-reviewer** fresh (never a fork, pinned fable), fix
   CRITICAL/MAJOR findings, wait for `CI / validate` green on the head commit,
   `gh pr merge --squash`.
5. Then start Phase 2: CONTRACT-002 (opus) first — it blocks SERVER-006/008/009/010
   and UI-002 — and in parallel SERVER-003 (opus) and CLI-001 (opus), which depend
   only on CONTRACT-001/INFRA-007. Spawn a sprint-planner contract for the batch.
