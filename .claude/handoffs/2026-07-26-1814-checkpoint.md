# Checkpoint: 2026-07-26 18:14

Supersedes `2026-07-26-1323-checkpoint.md` (Phase 1). That file is now historical —
**keep** it (only 2 files in `.claude/handoffs/`, well under the 5-file cleanup
threshold); its "Standing Gotchas" have since moved into the memory file
`corpus-orchestration-gotchas.md`, so it can be deleted once a third checkpoint lands.

See `issues/PLAN.md` for the authoritative issue table — not repeated here.

## State Summary

- **Merged to `main`** (`99c1ddc`): Phase 0 (PR #6, #7) and Phase 1 (PR #8) — all
  INFRA-*, SHARED-001, CONTRACT-001, SERVER-001/002, UI-001, AGENT-001 are `done`.
- **In flight**: branch `phase-2-server-cli`, **16 commits ahead of `main`, working
  tree clean, NOT pushed to origin**. No Phase 2 PR exists yet.
  - `done` (evaluator PASS, sprint-002): **CONTRACT-002, SERVER-003, CLI-001**
  - `in_progress`, batch just spawned (all opus, sprint-003 being written by the
    sprint-planner): **SERVER-004, SERVER-008, CLI-002**
  - `in_progress`, **ESCALATED TO USER**: **SERVER-012** (see below)
  - `todo`, newly filed: **CONTRACT-003** (P1) — request schemas with `.default()`
    render as required in the generated client.
- **Blocked on a user decision**: SERVER-012 only. Nothing else is blocked.

## SERVER-012 — escalation, awaiting the user

Four evaluator rounds. Full evidence in `issues/evals/SERVER-012-eval.md`; the
discriminator analysis is also recorded in `.claude/agents/server-dev.md` Domain
Knowledge. Condensed:

- Round 3's similarity-based `lacksKinship` fix regressed **TEST-63** and was
  **reverted** (`8e839f5`). The engine on the branch sits at the
  **evaluator-confirmed round-2 state** (`46fa225`) — a strict improvement over
  pre-round-2, not a regression.
- The **substitution class remains open**: the similarity proxy is the wrong
  discriminator. The evaluator's candidate replacement — a **survivor-location**
  test (is the surviving copy a _pre-existing duplicate_ the SERVER-002 ladder
  already handles, vs. a genuine in-place edit) — is recorded but **unimplemented**.
- **Three options for the user** (orchestrator recommendation first):
  1. **Re-scope SERVER-012 to the shipped round-2 behavior and file SERVER-013**
     for the survivor-location discriminator — scheduled **before SERVER-005**,
     which is the first issue to write through the anchor engine. _(recommended)_
  2. Authorize a **round 5** on the survivor-location approach now.
  3. **Defer** the substitution class to a later phase.
- Per the escalation ladder SERVER-012 has been on **fable** throughout (correct —
  it failed twice); every other Phase 2 issue ran on **opus** per its recommendation.

## Session Context (ephemeral — not in any issue file)

- **Worktrees cut by the Agent tool branch from `main`, not from the current
  branch.** Mid-phase this silently loses the branch's prior commits. Pre-create the
  worktree from the phase branch by hand (`git worktree add <path> <branch>`) and
  point the agent at it. This bit us once this session.
- **Sprint-002 adjudications are already persisted** — the nine "Open Conflicts"
  resolutions were written back into the individual issue files, not just
  `issues/sprints/sprint-002.md`. Do not re-litigate them.
- **Config canonical shape is settled and enforced in both readers** (server
  `SERVER-003` and CLI `CLI-001`): `port` defaults to **8765**; `host` is
  **schema-opaque**, with loopback enforced at bind time, not in the schema.
- Mechanics that used to live in the Phase 1 handoff now live in the memory file
  `corpus-orchestration-gotchas.md`: push verification via `git ls-remote`,
  `CORPUS_UI_PORT=5273` (5173 is a dev ssh tunnel), keeping **8765 free** for UI
  e2e, `node --import tsx` (never `npx`), scratch-dir prefixes, pid-targeted kills.
- Standing: **domain agents never commit**; the orchestrator harvests worktree
  patches (exclude `package-lock.json`, regenerate with `npm install`) and makes
  every commit.

## File Changes This Session (branch `phase-2-server-cli`)

- `packages/contract/**` — full §9.2 surface: 39 endpoints, query grammar, typed
  client; `internal_error` joined the `ApiError` union (CONTRACT-002)
- `apps/server/**` — Hono bootstrap, config, bearer auth, static UI serving
  (SERVER-003); anchor-engine cross-anchor pass + straddled-replacement routing,
  minus the reverted round-3 similarity fix (SERVER-012)
- `apps/cli/**` — bin, declarative command registry, workspace resolution,
  portless-config tolerance (CLI-001)
- `issues/sprints/sprint-002.md`, `issues/evals/CONTRACT-002|SERVER-003|CLI-001|SERVER-012-eval.md`
- `.claude/agents/server-dev.md` — Domain Knowledge: the survivor-location
  discriminator hypothesis

## Next Steps

1. **Put the SERVER-012 escalation to the user** and get a decision (option 1
   recommended: re-scope + file SERVER-013 ahead of SERVER-005).
2. **Wait on sprint-003** from the sprint-planner, then let SERVER-004, SERVER-008,
   CLI-002 run; harvest + commit each with its `[ISSUE-ID]` prefix.
3. Run `/test` and `/lint`, then spawn the **evaluator** on the finished batch;
   fix-loop max 3 rounds before escalating.
4. Once the Phase 2 issue set is `done`: push `phase-2-server-cli` (verify with
   `git ls-remote`), `/pr`, spawn **pr-reviewer** fresh on fable, wait for
   `CI / validate` green, `gh pr merge --squash`.
5. Schedule **CONTRACT-003** (P1) into a later Phase 2 batch — it is unblocked now.
