# Checkpoint: 2026-07-27 17:32

Phase 3 (`phase-3-ui`) opening batch is complete and evaluated. See `issues/PLAN.md` for
per-issue status; this file carries only what the plan, issue files, and evals do not.

## State Summary

- **Branch**: `phase-3-ui`, tip **6543280** (`[PLAN]` sprint-008 eval roll-up). **9 commits
  ahead of `main`** (verified `git rev-list --count main..HEAD`; the "~10" in the session
  summary was approximate). **No upstream configured — never pushed.** Working tree clean.
- **`main`** carries Phase 2 as squash **d604eca** (PR #9), merged after SHARED-002 sign-off
  (abb6b48).
- **Done in this batch (sprint-008)**: UI-002, CONTRACT-007, CONTRACT-009, SERVER-014,
  SERVER-020, SERVER-022, SERVER-023, CLI-008, INFRA-004 — 7 PASS, 2 PASS-with-waiver.
- **In flight**: sprint-planner for **sprint-009** — `issues/sprints/sprint-009.md` does **not
  exist on disk yet**. Batch: UI-003, UI-004, SERVER-016, SERVER-024, SERVER-025.
- **Ordering constraint inside that batch**: UI-003 depends on SERVER-024 (PLAN.md row 88) —
  spawn ui-dev for UI-003 only after SERVER-024 lands. UI-004, SERVER-016, SERVER-024,
  SERVER-025 are independently ready.
- **Remaining Phase 3 after sprint-009**: UI-005, UI-006, UI-007, UI-008, UI-009, UI-010,
  UI-011 (dependency chain in PLAN.md Phase 3 table).
- **All six anchor-engine adjudications are closed** (SERVER-012/013/014/018/020 + the
  reopen seam) — recorded in `.claude/agents/server-dev.md` Domain Knowledge, not here.

## Phase 3 PR — sign-off hold

The Phase 3 diff **does contain SPEC.md changes** (verified `git diff main..HEAD -- SPEC.md`:
2 hunks, from CONTRACT-007):

- **§6 Forms in turns** — whole-info-string fence matching, YAML grammar (`prompt` non-empty,
  `options` ≥ 1 / non-empty / distinct), form identity = carrying turn's timestamp (one form
  per turn), single-select answers verbatim from `options` + optional note.
- **§7 queue events** — `form.respond` payload `{threadId, formTs, option, note|null}`.

Per the **SHARED-002 process rule**, this PR is **not autonomous**: the user must sign off
("land") on the spec amendments before merge. Do not merge Phase 3 on green CI alone.

## Session Context (not in any persistent file)

- **TEST-76 waiver precedent**: the coupled `[CONTRACT-007][CONTRACT-009][SERVER-023]` commit
  (4ea3e4b) violated sprint-008's commit-separability criterion; waived by the orchestrator.
  Full rationale is recorded verbatim in **both** `issues/evals/CONTRACT-007-eval.md` and
  `issues/evals/CONTRACT-009-eval.md` (shared regenerated artifact set; a standalone contract
  commit breaks `apps/server` compilation and fails pre-commit; phase squashes anyway).
  Precedent chain: `[CONTRACT-005][SERVER-015]` → this. **Standing rule: coupled
  contract+consumer changes land as one green-unit commit.**
- **Standing rule**: an adjudication that changes user-observable behavior flows into SPEC.md
  in the **same commit** (spec-writer drafts; user signs off at the phase PR).
- **`timeout N cmd | tail` masks exit codes** — the pipeline reports `tail`'s status. Recorded
  in `issues/contract/009-*`'s log; do not re-derive.
- **API stalls are frequent this phase.** Remedy that works: `SendMessage` to the stalled agent
  with take-stock-first instructions — worktree state survives the stall; do not respawn blind.

### Operational facts (this phase's working set)

- Harvest from worktrees via **3-way patch** (`git apply -3`) against worktrees cut off the
  **phase tip**.
- CLI entry: `node --import tsx apps/cli/src/bin/corpus.ts` (never `npx`).
- Port **8765 stays unbound** (reserved); UI dev/e2e on `CORPUS_UI_PORT=5273`.
- e2e now **always starts its own Vite** (`reuseExistingServer: false`); pre-push exports a
  default `CORPUS_UI_PORT`.
- Combined coverage gate: `npm run coverage:merge` (INFRA-004). Currently **numerically
  identical to unit-only** — no server-backed e2e spec exists yet, so the merge is a no-op
  until one does. Do not read it as e2e coverage being proven.
- The reconcile perf test is now a **5 s order-of-magnitude guard** (load-flake fixed) — do
  not tighten it back into a wall-clock assertion.
- **Prettier every edited `.md` before committing** (`npx prettier --write`) — pre-commit
  blocks otherwise. This file has been formatted.
- Durable mechanics live in memory, do not re-derive:
  `~/.claude/projects/-Users-theophanerupin-code-corpus/memory/` —
  `corpus-orchestration-gotchas.md`, `per-phase-pr-babysit.md` (carries the Phase 2 lesson),
  `corpus-architecture-decisions.md`.

## File Changes This Session (main..HEAD, 192 files)

- `packages/contract/**` — forms surface + `form.respond` producer routes (CONTRACT-007);
  multipart `createThread` + declared 413 (CONTRACT-009).
- `apps/server/**` — rider consumption: warnings, reap-failed, `originTitle`, multipart, 413
  (SERVER-023); duplicate-survivor policy (SERVER-014); watcher tree-key by measurement +
  bounded flush (SERVER-020); nine PR #9 hardening findings, none waived (SERVER-022).
- `packages/kit/**`, `apps/ui/**` — data layer: hooks + SSE bridge (UI-002).
- `apps/cli/**` — five PR #9 hardening items (CLI-008).
- `scripts/merge-coverage.ts`, `vitest.config.ts`, pre-push hook — combined unit+e2e coverage
  gate and the port fix (INFRA-004).
- `SPEC.md` — §6/§7 form grammar (see sign-off hold above).

## Next Steps

1. **Collect sprint-009 from the planner** and confirm `issues/sprints/sprint-009.md` is on
   disk before spawning any implementer.
2. **Spawn SERVER-024, SERVER-025, SERVER-016, UI-004 in parallel** (worktree isolation for
   the overlapping server files); hold **UI-003** until SERVER-024 lands.
3. **Evaluate the batch**, then continue down the UI chain (UI-005 → UI-006 → UI-007; UI-009
   after UI-003).
4. **At phase end**: push `phase-3-ui` (verify with `git ls-remote`), `/pr`, fresh pr-reviewer
   on fable, CI green, **then user sign-off on the SPEC.md amendments**, then
   `gh pr merge --squash`.

## Housekeeping

`.claude/handoffs/` now holds **5** checkpoints. Once this one is consumed, the three oldest
(`2026-07-26-1323`, `2026-07-26-1814`, `2026-07-26-2208`) can be deleted.
