# Checkpoint: 2026-07-26 22:08

Supersedes `2026-07-26-1323-checkpoint.md` (Phase 1) and `2026-07-26-1814-checkpoint.md`
(mid-Phase-2). **Both are now historical and safe to delete** — their standing mechanics
moved into the memory file `corpus-orchestration-gotchas.md`, and the SERVER-012
escalation the 1814 file was written around is closed (see below). Directory holds 3
checkpoints; under the 5-file threshold, so pruning is optional, not required.

`issues/PLAN.md` is the authoritative issue table — not repeated here.

## State Summary

- **Branch `phase-2-server-cli`, 37 commits ahead of `main` (`99c1ddc`), working tree
  clean, NOT pushed to origin.** No Phase 2 PR exists yet.
- **Done** (all evaluator PASS, verdicts in `issues/evals/`): CONTRACT-002, CONTRACT-003,
  CONTRACT-004, SERVER-003, SERVER-004, SERVER-007, SERVER-008, SERVER-011, SERVER-012,
  SERVER-013, CLI-001, CLI-002.
- **In progress** (final Phase 2 batch, agents spawning; all **opus** per issue Model):
  SERVER-005 (doc write paths + git auto-commit), SERVER-009 (locks + job logs),
  CONTRACT-005 (board contract growth).
- **Next ready after that**: SERVER-006 → SERVER-010; CLI-003 (needs SERVER-005+006),
  CLI-004 (needs SERVER-008+009).
- **Deferred, not blocking**: SERVER-014 (P2, **fable**) — duplicate-survivor policy,
  the TEST-64/66 tension left open by SERVER-013. Phase 3 (UI-002…) is untouched.
- **Nothing is blocked on the user.** No SPEC.md changes are in this branch's diff, so the
  Phase 2 PR should not need a sign-off hold.

## Session Context (ephemeral — not in any persistent file)

- **The anchor saga is CLOSED.** SERVER-012 was re-scoped to its round-2 behavior by user
  decision; SERVER-013 implemented the authorized **relocation-evidence** discriminator
  (INSERT-overlapping verbatim survivor voids a rewritten slice; EQUAL survivors keep
  mapper trust). Five adjudications now stand recorded in `.claude/agents/server-dev.md`
  Domain Knowledge — **treat them as settled**; SERVER-014 is the only sanctioned reopening,
  and it must not disturb them. **Similarity thresholds are permanently off the table.**
- **Sprint contracts 001–004** (`issues/sprints/`) carry adjudications that were written
  back into the individual issue files. Do not re-litigate. **Sprint-005 for the final batch
  is not yet on disk** — the sprint-planner had not delivered it at checkpoint time.
- **Model actuals**: everything ran on **opus** except the anchor engine
  (SERVER-002/012/013 on **fable**) — matching every recommendation. **No escalation-ladder
  corrections were needed this phase**; recommendations are calibrated as written.
- Standing mechanics live in memory `corpus-orchestration-gotchas.md` and are current:
  worktrees branch from `main` (pre-create by hand mid-phase), harvest via
  `git apply -3` excluding `package-lock.json`, `git push origin <branch>` + `git ls-remote`
  verification, **8765 reserved for UI e2e**, `CORPUS_UI_PORT=5273`, `node --import tsx`,
  per-agent scratch prefixes, pid-targeted kills only, `GIT_*` env sanitization.
- Standing: **domain agents never commit** — the orchestrator harvests and commits.

## File Changes Since the Last Checkpoint (21 commits, 169 files)

- `apps/server/**` (77 files) — SQLite projection + FTS + rebuild/doctor (SERVER-004),
  queue event store + long-poll + projection-backed mirror (SERVER-008), watcher/SSE
  invalidation (SERVER-007), collection query filters/FTS/`needs=me` (SERVER-011),
  relocation-evidence anchor fix (SERVER-013)
- `apps/cli/**` (34 files) + `docs/cli.md` — `corpus init`, server lifecycle verbs (CLI-002)
- `packages/contract/**` (23 files) — no-defaults-in-request-bodies convention (CONTRACT-003),
  explicit `required` on every request body (CONTRACT-004)
- `scripts/workspace-template.*`, `docs/workspace-template.md` — exhaustive install contract
  for what `corpus init` generates (AGENT-001 follow-up)
- `issues/**` — sprint-003/004, eight new eval verdicts, SERVER-013/014 + CONTRACT-004/005
  filed, PLAN.md status updates
- `.claude/agents/server-dev.md` — the fifth (SERVER-013) anchor adjudication

## Next Steps

1. Let SERVER-005 / SERVER-009 / CONTRACT-005 run; have the sprint-planner land
   **sprint-005** before or alongside them. Harvest + commit each with its `[ISSUE-ID]`.
2. Run `/test` and `/lint`, then spawn the **evaluator** per issue; fix-loop max 3 rounds.
3. Then the tail of Phase 2 in dependency order: **SERVER-006 → SERVER-010**, and
   **CLI-003 / CLI-004** once their server deps are done.
4. Cut the Phase 2 PR: push `phase-2-server-cli` (verify with `git ls-remote`), `/pr`, spawn
   **pr-reviewer** fresh on fable, wait for `CI / validate` green, `gh pr merge --squash`.
5. Leave **SERVER-014** for after the merge unless the evaluator surfaces it in a Phase 2
   verdict.

## Inconsistencies Noted at Checkpoint

- `issues/server/009-locks-jobs.md` says **Status: todo** while `issues/PLAN.md` lists
  SERVER-009 as **in_progress**. Reconcile when the agent reports.
- The SERVER-013 line in server-dev Domain Knowledge is dated **2026-07-27** (UTC drift past
  local midnight); everything else is dated 2026-07-26. Cosmetic.
