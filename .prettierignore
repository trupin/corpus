node_modules/
dist/
dist-package/
build/
coverage/
coverage-raw/
design/
SPEC.md
issues/
*.generated.ts
packages/contract/openapi.json
# The workspace template is product code: its bytes are what `corpus init`
# installs into a user's workspace, so Prettier must never rewrap or re-mark it.
assets/workspace/
# The editor's round-trip corpus. Each fixture is markdown in the exact shape
# the serializer emits, and the suite asserts `serialize(parse(md)) === md` byte
# for byte — Prettier rewrapping one would make the assertion test Prettier's
# markdown style instead of the serializer's.
apps/ui/src/editor/markdown/fixtures/
# Vendored, not written here (INFRA-030). These four files are copied byte for
# byte from danyuchn/asd-ste100-skill, and PROVENANCE.md next to them promises a
# refresh is a straight refetch. Prettier rewrapping one would make every future
# refresh a conflict against formatting we introduced. PROVENANCE.md itself is
# ours and stays formatted, so it is not listed.
.claude/skills/asd-ste100/SKILL.md
.claude/skills/asd-ste100/LICENSE
.claude/skills/asd-ste100/references/
.claude/skills/asd-ste100/examples/
# Captured clipboard payloads (UI-042). These are bytes a word processor put on
# the clipboard, and the paste path is asserted against them: pretty-printing
# the markup would insert whitespace text nodes between the tags and change what
# the parser sees, so the test would stop describing the real paste.
apps/ui/src/editor/fixtures/
# Generated per rehearsal pass by `npm run rehearse` (INFRA-033); committed as
# the pass's durable artifact. Prettier rewrapping it would make every pass a
# formatting diff on top of the real one.
rehearsals/scorecard.md
rehearsals/out/
