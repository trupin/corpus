Plain prose, then **bold**, then *italic*, then ***both at once***.

Inline `code()` and `a `` b` inside a sentence, plus ~~struck through~~ text.

A word with **bold *nested italic* inside** it.

snake_case and file_name_two stay unescaped; 2 * 3 = 6 does too.
