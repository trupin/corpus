Plain prose, then **bold**, then *italic*, then ***both at once***.

Inline `code()` and `a `` b` inside a sentence, plus ~~struck through~~ text.

A word with **bold *nested italic* inside** it.

snake_case and file_name_two stay unescaped; 2 * 3 = 6 does too.

**alpha beta** gamma delta, and alpha **beta gamma** delta: the space between a
run and its neighbour sits outside the markers, where markdown can spell it.

**link:** [[doc_mbc52nvo]]**6.4%** this week, and ~~gone~~ next.

**a** \*star\* and **a** \_under\_ and **a** \`tick\` and **a** \\slash.

**a** \<tag> and **a** & sign and **a** \[link](x) and **a** \~\~struck\~\~,
each with its marker outside the emphasis and its neighbour escaped with a
backslash rather than encoded as a character reference.
