A fenced block with a language:

```ts
export function add(a: number, b: number): number {
  return a + b;
}
```

A fenced block with none:

```
plain text
  indented line
```

An empty one:

```
```
