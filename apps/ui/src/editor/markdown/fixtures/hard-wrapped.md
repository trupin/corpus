# Office hours

Tomorrow is a Wednesday, so the office opens an hour later than the rest of
the week. The badge readers on the north door stay on the old schedule until
facilities reflashes them, which is why the lobby desk is staffed from seven.

A wrapped paragraph can carry every inline construct across its break:
**bold text that spans the wrap**, *italic likewise*, a [link whose text is
split](https://example.com/rates) and a `code span across
lines`.

> A blockquote the agent wrapped at the same width, so its continuation line
> carries the same soft newline every paragraph above it does.

- A list item that is itself hard wrapped, because the item's prose is longer
  than the column the agent writes to.
- A short one.

A line ending in a deliberate hard break\
is a different thing entirely, and stays a break.

```txt
A fenced block keeps its own line endings:
this is the second line, and it is not a soft wrap.
```
