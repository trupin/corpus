// Placeholder module so the `@corpus/contract/client` subpath resolves from
// consumers before CONTRACT-001 lands the real typed client factory here.
export const CLIENT_PACKAGE_PATH = "@corpus/contract/client";
